{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "./compressao-dados/rle/main.go", // Executa o pacote no diretório do projeto
            "args": [] // Argumentos de linha de comando (opcional)
        }
    ]
}